/* Theme entry: compiled with includeTheme: true. Defines every global
   :root token the four panel entries below consume. No component markup
   of its own — the theme JSON (theme.json) is the actual source of the
   tokens. */
