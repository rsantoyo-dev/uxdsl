/** Shared product guidance for human documentation and AI implementation guides. */
export default function SpacingPrinciple() {
  return (
    <p>UXDSL builds on standard CSS. Spacing defines the base spacing values, and Density makes that spacing responsive. <strong>Prefer Density for component spacing</strong>, use Spacing when a stable value is intentional, and use CSS directly when finer control is needed. This is the recommended authoring approach, not a compiler restriction.</p>
  )
}
