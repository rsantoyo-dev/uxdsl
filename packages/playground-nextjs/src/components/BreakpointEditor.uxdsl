.editor-content {
  padding: density(6);
  display: grid;
  gap: density(4);
  max-height: 60vh;
  overflow-y: auto;
}

.font-selector {
  display: flex;
  flex-direction: column;
  gap: density(2);
}
